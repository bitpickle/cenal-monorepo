import { Roles } from "../types/Roles";

const routeRoles = new Map<string, string[]>();

// Example: routeRoles.set("/avaliacoes",[Roles.VEREADOR,Roles.ASSESSOR_JURIDICO]);

export default routeRoles;